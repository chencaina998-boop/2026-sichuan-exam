# STAGE2_BRIEF.md — 阶段 2 作业书（**高质量版**）

> **本文是阶段 2 的执行说明。目标是产出最终的学习资料。**
>
> ⚠️ **本次不设成本限制** —— 请按**最高质量**标准来做，不要为了省 token 而简化。
> 需要读什么就读什么，需要看原图就看原图，需要迭代就迭代。

---

## 0. 背景：你要做的是什么

考生（正在备考 2026 年下半年四川教师公招）**已经有**两份学习资料：

- `out/心理学12_精讲全解.html` —— 3 幕 16 节逐点精讲
- `out/心理学12_闯关记忆手册.html` —— 22 关闯关 + 擂台 + 错题本 + 闪卡

**但考生不满意**，她的原话是：

> "我不要知识点零散的堆砌。我需要知识点能够串联起来，
> 在我的脑海当中形成一幅生动的画面，让我在做题的时候能够游刃有余。"
>
> "因为我的基础比较差，所以有的知识点我需要你转化成通俗易懂的内容。
> 能够让我记忆起来比较轻松。"

**所以你的任务**：在她现有两份资料的基础上，做一份**真正好懂、好记、能做对题**的学习资料。

---

## 1. 素材：**全部都可以读**

### 1.1 核心素材（**必读**）

**① 原始素材（2 个）**

| 文件 | 大小 | 内容 |
|---|---|---|
| `out/transcript.txt` | 126 KB | **老师讲的每一句话（45,541 字）← 真相源** |
| `out/slides_unique.txt` | 69 KB | 102 张幻灯片 OCR |

**② Hoplite 上一轮工作的产出（6 个）—— 全部必读**

这是前一个 Agent 在阶段 0 / 阶段 1 做出来的成果，**是本次制作的重要基础**：

| 文件 | 大小 | 内容 | 怎么用 |
|---|---|---|---|
| **`out/slides_vision.md`** | 185 KB | **102 张幻灯片逐张视觉记录**（手写/颜色/结构/插图 + OCR 对照） | ⭐ **核心素材**，手写批注全靠它 |
| **`out/OCR_GAPS.md`** | 22 KB | OCR 漏掉信息的汇总（按手写/颜色/结构/插图四类） | ⭐ 快速查「OCR 缺了什么」 |
| `out/frames_vision.md` | 22 KB | F001~F040 帧记录（开头 00:27~11:51，比 102 张更细） | 开头部分的补充细节 |
| `out/STAGE0_INVENTORY.md` | 16 KB | **阶段 0 清点报告**：45/45 份文件逐项清点（大小/内容量/内容概括/结构） | 📊 **先读这个**，快速建立全局认识 |
| `out/slides_vision_TEST.md` | 10 KB | 读图流水线 10 张试跑记录（S001~S010） | 参考格式（内容与 slides_vision.md 重复） |
| `out/TEST_IMAGES_RESULT.md` | 4 KB | 读图能力测试结果 | 💡 含关键结论：看图要用 `functions.shell` + `image_path` |

> 💡 **建议先读 `STAGE0_INVENTORY.md`** —— 它把全部素材的内容量、结构和
> **质量问题**都列清楚了（比如「OCR 有形近字错误和乱码，转写仍需术语校对」）。

### 1.2 辅助素材（需要交叉核对时读）

| 文件 | 大小 | 用途 |
|---|---|---|
| `out/slides_index.md` | 255 KB | 691 行「画面 ↔ 讲解」对照表 |
| `out/slides_index.json` | 815 KB | 结构化版本，含更完整的讲解字段 |
| `out/ocr_all.jsonl` | 566 KB | 691 帧逐帧 OCR |
| `out/slides/manifest.md` | 43 KB | 102 张幻灯片清单（编号/时间/图文件/OCR） |
| `out/uniq_meta.json` | 58 KB | 帧时间轴元数据 |
| `out/asr_raw.jsonl` | 142 KB | 原始转写（含秒级时间戳） |


### 1.3 旧版成品（**必读** —— 你要在它基础上改进）

⚠️ **这三份 HTML 内嵌了大量 base64 图片，直接读会浪费上下文。先剥离：**

```bash
python3 - <<'EOF'
import re, os
for f in ['心理学12_精讲全解.html', '心理学12_闯关记忆手册.html', '幻灯片总览_高清版.html']:
    p = 'out/' + f
    if not os.path.exists(p):
        print(f, '不存在'); continue
    h = open(p, encoding='utf-8').read()
    before = len(h)
    h2 = re.sub(r'data:image/jpeg;base64,[A-Za-z0-9+/=]+', '[图]', h)
    out = '/tmp/' + f + '.txt'
    open(out, 'w', encoding='utf-8').write(h2)
    print(f'{f}: {before//1024} KB → {len(h2)//1024} KB  ({out})')
EOF

cat /tmp/心理学12_精讲全解.html.txt
cat /tmp/心理学12_闯关记忆手册.html.txt
cat /tmp/幻灯片总览_高清版.html.txt
```

**读完后要能回答**：
- 旧版**好在哪里**？（保留）
- 旧版**差在哪里**？（改进）
- 旧版的**目录结构**是什么？（可以推翻重来，但要说明理由）

### 1.4 原图（**需要时看，这是你的优势**）

**先装环境**（否则会报 `ModuleNotFoundError: No module named 'PIL'`）：

```bash
bash setup.sh && source /etc/profile.d/pipeline.sh
```

**解压**：

```bash
mkdir -p ./sl && cd ./sl
unzip -q ../out/frames/slides_102.zip -d slides/
ls slides/*.jpg | wc -l        # 102
```

**用 `functions.shell` 的 `image_path` 参数看单张**：

```
functions.shell(command="ls sl/slides/S050.jpg", image_path="sl/slides/S050.jpg")
```

> ⚠️ **两个实测过的坑（上一轮测试踩到）**：
>
> **① `image_path` 必须用「工作区相对路径」，不能用 `/tmp/...` 绝对路径。**
> 用绝对路径会报错：`shell image_path must stay inside the workspace`
> → 所以解压到**工作区内的目录**（比如 `./sl/`），不要解压到 `/tmp/`。
>
> **② 必须先 `bash setup.sh`**，否则 Pillow 没装，
> 想裁剪/压缩图片时会报 `ModuleNotFoundError: No module named 'PIL'`。
> （直接看图不需要 Pillow，但裁剪放大需要。）
>
> **③ 必须用 `functions.shell` + `image_path`。**
> ❌ 不要用 `functions.display_multimedia`（只返回链接，不提供视觉输入）。
> 详见 `HOW_TO_READ_IMAGES.md`。

**什么时候该看原图**：
- `slides_vision.md` 里标了「认不准」的地方
- 图表结构复杂、光看文字描述没把握
- 需要确认颜色/高亮/手写位置
- **一次只能看 1 张** —— 按需看，别贪多

---

## 2. 怎么做（**不设限制，但要有章法**）

### 2.1 建议：分幕做，每幕一个会话

本讲有 4 幕：

| 幕 | 内容 | 对应幻灯片 |
|---|---|---|
| 第一幕 | 学习分类 | S001 ~ S014 |
| 第二幕 | 四大流派总览 | S015 ~ S017 |
| 第三幕 | 行为主义（华生 / 桑代克 / 巴甫洛夫） | S018 ~ S085 左右 |
| 第四幕 | 溯源拔高 | S086 ~ S102 |

**每幕一个会话** —— 这样每一幕都能做深做透，而不是赶工。
每幕做完 commit 一次，然后开新会话做下一幕。

> 如果你判断一个会话能做完 4 幕且质量不降，也可以一次做完。
> **以质量为准，不以省 token 为准。**

### 2.2 建议两轮

**第一轮**：产出初稿
**第二轮**：自审修订 —— 对照 `REQUIREMENTS.md` 六条标准逐条检查，把不合格的地方改掉

**自审时重点查**：
- 有没有知识点漏了？（对照 `slides_unique.txt` + `transcript.txt` 逐条数）
- 有没有哪个术语没解释就用了？
- 有没有哪个知识点没配题目？
- **手写批注里的东西有没有漏掉？**

---

## 3. 要做什么

**验收标准在 `REQUIREMENTS.md` 第三节，请回去读**（六条：覆盖度 / 通俗度 / 记忆度 /
做题度 / 体验度 / 形式）。

### 3.1 考生的核心诉求（原话）

> "我不要知识点零散的堆砌。我需要知识点能够串联起来，
> 在我的脑海当中形成一幅生动的画面，让我在做题的时候能够游刃有余。"

> "因为我的基础比较差，所以有的知识点我需要你转化成通俗易懂的内容。
> 能够让我记忆起来比较轻松。"

> "通过你的帮助，让我的这一科目的学习变得不那么枯燥，
> 而是有趣且高效。"

### 3.2 六条验收标准（摘自 REQUIREMENTS.md）

1. **覆盖度 —— 不许漏**
   覆盖每个知识点、全部真题及手写/板书/口述补充。
   交付时**必须汇报数字**：`知识点总数 N / 已覆盖 N / 未覆盖 0`

2. **通俗度 —— 把考生当小学生**
   每个术语和知识点都应让零基础读者理解，而非只改写部分内容。

3. **记忆度 —— 不止当下听懂**
   目标是"三天后能自己讲出来"。要有**比方、记忆线索、自测**。

4. **做题度 —— 能够运用**
   每个知识点落到具体题目，**真题附老师的解题推导**。

5. **体验度 —— 不疲惫、不厌倦**
   比现有版本更愿意读。原要求提到 **Apple 2026 的色彩与设计语言**。

6. **形式 —— 由制作者决定**
   结构和载体可自主选择，但必须更好懂、更愿意看、更记得住。

### 3.3 素材怎么用

| 素材 | 用途 |
|---|---|
| `out/transcript.txt` | **真相源** —— 老师原话，所有知识点的依据 |
| `out/slides_unique.txt` | 幻灯片上的印刷内容 |
| **`out/slides_vision.md`** | **手写批注、颜色高亮、图表结构、插图** —— OCR 抓不到的 |
| `out/OCR_GAPS.md` | 上面这些的汇总版 |
| 旧版三份 HTML | 要改进的对象 |
| `out/frames/slides_102.zip` | 需要时看原图核对 |

> ⭐ **重点**：`slides_vision.md` 和 `OCR_GAPS.md` 是核心资产。
> 老师写在 PPT 上的手写批注（比如准备律那页的三条红字结论、
> 桑代克三条公律的树状图、巴甫洛夫那页的「条件S₁/中性S/条件反1」）
> **只有这里有**，`slides_unique.txt` 的 OCR 全丢了。
> **这些手写批注往往是解题关键，不能漏。**

### 3.4 本讲范围（**重要**）

学习分类 + 四大流派总览 + 行为主义（华生 / 桑代克 / 巴甫洛夫）+ 溯源拔高。

**斯金纳、班杜拉、认知主义、建构主义、人本主义老师没讲**（原话"明天讲"）——
**没讲就是没讲，不要自己补内容。**

---

## 3.5 设计规范（**重要 —— 小样测试发现设计偏简单**）

> 上一轮小样测试的**内容质量很好**（用上了批注、比方贴切、真题带解法），
> **但设计偏简单**：没有毛玻璃质感、没有进度反馈、配色偏通用。
> 本次请按下面的规范做。

### 3.5.1 色彩（Apple 官方色系）

```css
:root{
  --bg:      #f5f5f7;              /* 页面背景，浅灰 */
  --card:    rgba(255,255,255,.72);/* 卡片，半透明 */
  --ink:     #1d1d1f;              /* 正文，近黑 */
  --ink2:    #48484a;              /* 次要文字 */
  --ink3:    #86868b;              /* 弱化文字 */
  --blue:    #0071e3;              /* 主色 */
  --green:   #1d9a52;              /* 正确 / 有意义 */
  --red:     #e0342b;              /* 错误 / 机械 */
  --amber:   #c77700;              /* 提示 / 比方 */
  --purple:  #7a4fd6;              /* 手写批注 */
  --radius:  20px;
}
```

❌ **不要用** Tailwind 默认蓝 `#2563eb` 这类通用色 —— 要 Apple 的 `#0071e3`。

### 3.5.2 字体与阅读（**考生的核心诉求是「不累」**）

| 属性 | 要求 |
|---|---|
| 正文字号 | **17px**（移动端 16.5px） |
| 行高 | **1.85** |
| 字间距 | 标题 `-0.02em` |
| 段落间距 | `margin: 0 0 10px` |
| 最大宽度 | **820px**（太宽眼睛累） |
| 字体栈 | `-apple-system, BlinkMacSystemFont, "SF Pro SC", "PingFang SC", "Microsoft YaHei", sans-serif` |
| 抗锯齿 | `-webkit-font-smoothing: antialiased` |

### 3.5.3 质感（Liquid Glass）

```css
.card{
  background: rgba(255,255,255,.72);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border: 1px solid rgba(0,0,0,.08);
  border-radius: 20px;
}
```

### 3.5.4 必备的交互元素

| 元素 | 作用 |
|---|---|
| **顶部滚动进度条** | 让考生知道「还剩多少」，减少焦虑 |
| **折叠式自测**（`<details>`） | 先想再看答案，主动回忆才有效 |
| **卡片式分块** | 每块一个知识点，不要长墙 |
| **色块区分** | 说人话=琥珀 / 比方=紫 / 术语=蓝 / 坑=红 / 钩子=绿 |

### 3.5.5 内容块类型（建议每块都有）

```html
<!-- 说人话：琥珀色底 -->
<div class="blk hl-amber">…</div>

<!-- 打个比方：紫色标签 + 白底 -->
<div class="blk">…</div>

<!-- 手写批注：紫色左边框，独立成块 ⭐ 这是你的独家价值 -->
<div class="hw">
  <div class="who">✍️ S050 · 紫笔手写</div>
  <p>…</p>
</div>

<!-- 最容易错的地方：红色底 -->
<div class="blk hl-red">…</div>

<!-- 记忆钩子：绿色底 + 💡 -->
<div class="hook"><span>💡</span><span>…</span></div>

<!-- 真题：白卡片 + 蓝色解法区 -->
<div class="q">…</div>
```

### 3.5.6 反面清单（别做这些）

- ❌ 长段落墙（超过 5 行不分段）
- ❌ 术语套术语（"学习是指个体经由练习而产生的…" 这种不算解释）
- ❌ 全篇一个颜色
- ❌ 手写批注混在正文里（要单独成块，视觉上区分开）
- ❌ 没有进度反馈（考生不知道还有多少）
- ❌ 字号小于 15px

> 💡 **参考**：`out/心理学12_精讲全解.html` 和 `out/心理学12_闯关记忆手册.html`
> 是旧版，可以看它们的**结构**，但**设计要向上面这套规范靠**。

---

## 4. 交付

```bash
git add -A
git commit -m "feat: 心理学12 学习资料（阶段 2 成品）"
git push -u origin HEAD
```

**汇报必须包含**：

| # | 项目 | 内容 |
|---|---|---|
| 1 | 你做了什么 | 形式、结构、设计思路 |
| 2 | **覆盖度数字** | 知识点总数 ___ / 已覆盖 ___ / 未覆盖 ___ |
| 3 | 产出文件与大小 | |
| 4 | 比旧版好在哪 | **逐条对照六条标准**说明改进 |
| 5 | 读图素材用上了吗 | 举 3 个「手写批注进了资料」的具体例子 |
| 6 | 限制或没做完的 | 如实说，不要编 |

---

## 5. 一句话

> **读透全部素材（含旧版和原图）→ 分幕做深做透 → 对照六条标准自审修订 →
> 交付一份真正好懂、好记、能做对题的资料。**
>
> **不设成本限制。以质量为准。**
