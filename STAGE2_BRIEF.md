# STAGE2_BRIEF.md — 阶段 2 作业书（制作学习资料）

> **本文是阶段 2 的执行说明。** 目标是产出最终的学习资料。
>
> ⚠️ **本文有明确的「省钱要求」** —— 请严格按第 1 节的**只读清单**来读文件。
> 仓库里素材总共 11 MB，但**你只需要读 424 KB**（省 96%）。

---

## 1. ⭐ 只读这 5 个文件（**别读别的**）

```bash
git fetch origin && git merge origin/main

# 只读这 5 个：
cat out/slides_vision.md      # 185 KB  102 张幻灯片的逐张视觉记录（含手写/颜色/结构/插图）
cat out/transcript.txt        # 126 KB  老师讲的每一句话（真相源）
cat out/slides_unique.txt     #  69 KB  102 张幻灯片 OCR
cat out/OCR_GAPS.md           #  22 KB  OCR 漏掉信息的汇总（手写/颜色/结构/插图四类）
cat out/frames_vision.md      #  22 KB  F001~F040 补充细节（开头部分）
```

**小计 424 KB。这就是你需要的全部素材。**

---

## 1.5 ⭐ 另外要看「旧版成品」——**必须先剥离 base64**

考生明确要求：**「在你现有的制作完成的闯关记忆手册和精讲全解的基础之上，再进一步的组织和设计一下语言」**。
所以你需要**先看看旧版长什么样**，才知道要改进什么。

⚠️ **但这三个 HTML 里内嵌了大量 base64 图片，直接读会烧掉巨量 token。**
**必须先剥离：**

```bash
python3 - <<'EOF'
import re, os
for f in ['心理学12_精讲全解.html', '心理学12_闯关记忆手册.html', '幻灯片总览_高清版.html']:
    p = 'out/' + f
    if not os.path.exists(p):
        print(f, '不存在'); continue
    h = open(p, encoding='utf-8').read()
    before = len(h)
    h2 = re.sub(r'data:image/jpeg;base64,[A-Za-z0-9+/=]+', '[图]', h)
    out = '/tmp/' + f + '.txt'
    open(out, 'w', encoding='utf-8').write(h2)
    print(f'{f}: {before//1024} KB → {len(h2)//1024} KB  ({out})')
EOF

# 然后读剥离后的文件
cat /tmp/心理学12_精讲全解.html.txt        # 剥离后约 70 KB
cat /tmp/心理学12_闯关记忆手册.html.txt    # 剥离后约 35 KB
cat /tmp/幻灯片总览_高清版.html.txt        # 剥离后约 300 KB
```

| 文件 | 原始 | 剥离后 | 值得读吗 |
|---|---|---|---|
| `心理学12_精讲全解.html` | 682 KB | **~70 KB** | ✅ **值得** —— 旧版精讲，你要在它基础上改进 |
| `心理学12_闯关记忆手册.html` | 2,155 KB | **~35 KB** | ✅ **值得** —— 旧版闯关，同上 |
| `幻灯片总览_高清版.html` | 6,103 KB | **~300 KB** | ⚠️ **大部分冗余** —— 它的内容 = 102 张的（OCR + 讲解），已被 `slides_unique.txt` + `transcript.txt` 覆盖。**只需看它的结构（有多少卡片、每卡有哪些字段），不必逐条读** |

**建议**：前两个**完整读**（约 105 KB）；第三个**只读开头 50 行 + 统计卡片数**即可。

**所以新增阅读量约 105~110 KB**（不是 8.9 MB）。

---

## 1.6 ❌ 不要读这些（信息冗余或体积过大）

| 文件 | 大小 | 为什么不读 |
|---|---|---|
| `out/slides_index.json` | 815 KB | 索引，内容与上面重复 |
| `out/slides_index.md` | 255 KB | 同上 |
| `out/ocr_all.jsonl` | 566 KB | 逐帧 OCR，已被 `slides_unique.txt` + `slides_vision.md` 覆盖 |
| `out/asr_raw.jsonl` | 142 KB | 原始转写，已被 `transcript.txt` 覆盖 |
| `out/uniq_meta.json` | 58 KB | 帧时间轴元数据 |
| `out/slides/manifest.md` | 43 KB | 幻灯片清单 |
| `out/STAGE0_INVENTORY.md` | 15 KB | 阶段 0 清点报告，不是素材 |

> 💡 如果确实需要看某张幻灯片原图：
> 解压 `out/frames/slides_102.zip`，用 `functions.shell` 的 `image_path` **单张**读。
> **一次只读一张**，别批量。

---

## 1.7 汇总：本次总共读多少

| 类别 | 大小 |
|---|---|
| 核心素材（5 个） | 424 KB |
| 旧版成品（剥离后，3 个） | ~110 KB |
| **合计** | **~534 KB** |

（对比：仓库全部素材 11 MB —— **省了 95%**）

---

## 2. 一次做完，**不要分批**

⚠️ **这条也是省钱要求。**

**分批做不会省钱，反而更贵** —— 每开一个新会话，都要**重新加载全部素材**。
分 4 批做 = 素材被读 4 遍 = 输入成本翻 4 倍。

**所以：在一个会话里一次做完。**

如果上下文装不下，**优先压缩输出**（少写一点），而不是拆成多批。

---

## 3. 产出规模上限

**总量控制在 100 KB 以内**（HTML 或 Markdown 都算）。

理由：输出 token 也是钱。写得太长既费钱，考生也不一定看得完。

**宁可精炼，不要堆砌。** 重点是把知识讲透、好记，不是字数多。

---

## 4. 要做什么

**验收标准在 `REQUIREMENTS.md` 第三节，请回去读**（六条：覆盖度 / 通俗度 / 记忆度 /
做题度 / 体验度 / 形式）。

**形式、结构、载体全部由你决定。**

### 关键要求（摘自 REQUIREMENTS.md）

1. **覆盖度**：不许漏知识点。交付时**汇报数字**：`知识点总数 N，已覆盖 N，未覆盖 0`
2. **通俗度**：把考生当小学生 —— 每个术语都要让人一遍就懂
3. **记忆度**：目标不是"读的时候懂了"，而是"**三天后还能自己讲出来**"
4. **做题度**：每个知识点落到具体题目，真题附老师的解题推导
5. **体验度**：比现有版本更愿意读；`apple 的 2026 年最新的色彩搭配和设计语言`
6. **形式**：由你决定

### 素材怎么用

| 素材 | 用途 |
|---|---|
| `out/transcript.txt` | **真相源** —— 老师原话，所有知识点的依据 |
| `out/slides_unique.txt` | 幻灯片上的印刷内容 |
| **`out/slides_vision.md`** | **手写批注、颜色高亮、图表结构、插图** —— OCR 抓不到的 |
| `out/OCR_GAPS.md` | 上面这些的汇总版（快速查） |
| `out/frames_vision.md` | 开头部分（00:27~11:51）的更细记录 |

> ⭐ **重点**：`slides_vision.md` 和 `OCR_GAPS.md` 是这次新增的核心资产。
> 老师写在 PPT 上的手写批注（比如准备律那页的三条红字结论、
> 桑代克三条公律的树状图）**只有这里有**，`slides_unique.txt` 的 OCR 全丢了。

### 本讲范围

学习分类 + 四大流派总览 + 行为主义（华生 / 桑代克 / 巴甫洛夫）+ 溯源拔高。

**斯金纳、班杜拉、认知主义、建构主义、人本主义老师没讲**（原话"明天讲"）——
**没讲就是没讲，不要自己补内容。**

---

## 5. 交付

```bash
git add -A
git commit -m "feat: 心理学12 学习资料（阶段 2 成品）"
git push -u origin HEAD
```

**汇报必须包含**：

| # | 项目 | 内容 |
|---|---|---|
| 1 | 你做了什么 | 形式、结构、设计思路 |
| 2 | **覆盖度数字** | 知识点总数 ___ / 已覆盖 ___ / 未覆盖 ___ |
| 3 | 产出大小 | ___ KB |
| 4 | 比旧版好在哪 | 具体说明"更好懂"和"更记得住"的改动 |
| 5 | 限制或没做完的 | 如实说，不要编 |
| 6 | **本次读了哪些文件** | 确认只读了第 1 节那 5 个 |

---

## 6. 一句话

> **只读 5 个文件（424 KB）→ 一个会话做完 → 产出 ≤100 KB → 满足六条验收标准。**
