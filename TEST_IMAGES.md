# TEST_IMAGES.md — 读图能力测试（**只做测试，不要做手册**）

> ## 这是测试任务，不是制作任务
>
> **严禁**：不要制作任何手册、不要改任何现有文件、不要跑 `setup.sh`、不要转写、不要 OCR。
> 只做**读取和核对**，然后**如实汇报**。
>
> **不许编造。** 看不见就说看不见。做不到就说做不到，并贴出**原始报错**。

---

## 背景

这个仓库里有一份网课视频的深度分析产物，其中包含**幻灯片原图和视频帧**。
现在要测试：**你能不能真正「看到」这些图片里的内容？**

这很重要 —— 因为最终要做的学习资料，需要把 PPT 上的知识点讲全。
如果连图都读不了，就必须改走文字路线。

---

## 准备

```bash
mkdir -p /work/imgtest && cd /work/imgtest
unzip -q out/frames/slides_102.zip -d slides/ 2>/dev/null || python3 -m zipfile -e out/frames/slides_102.zip ./slides/
ls slides/*.jpg | wc -l        # 预期 102
```

---

## 测试 1 · 单张读图（最关键的一步）

**只读一张**：`slides/S001.jpg`

**回答**：
1. 这张图是**什么类型的画面**？（PPT 幻灯片 / 视频截图 / 其他）
2. 图上的**标题**是什么？
3. **抄录图上你能看到的所有文字**（尽量完整，包括小字）
4. 有没有**手写批注**？如果有，写的是什么？
5. 右下角有没有**水印或编号**？

> ⚠️ **不许用文件名、不许用仓库里的 OCR 文本（`out/slides_unique.txt`）来推断答案。**
> 这一步只测「你的眼睛能不能看见图上的字」。

---

## 测试 2 · 连续读 3 张

读 `slides/S001.jpg`、`slides/S002.jpg`、`slides/S003.jpg`。

**回答**：三张的标题分别是什么？（必须三张都答）

---

## 测试 3 · 找出上限（逐级加码，失败就停）

每一级都**只用一次请求**把该数量的图交给模型：

| 级别 | 数量 | 要回答 |
|---|---|---|
| A | 5 张 | 五张的标题，按顺序 |
| B | 10 张 | 十张的标题，按顺序 |
| C | 20 张 | 二十张的标题，按顺序 |
| D | 50 张 | 只报告**实际看到**了几张，没看到的明确说「没看到」 |

**某级失败就停下来，不要跳级，不要重试超过 1 次。**
把失败那一级的**原始报错/限制提示原文**贴出来。

---

## 测试 4 · 换个方式读（看有没有别的路）

如果你在测试 3 的某一级失败了，试试这些替代方案，看哪个可行：

1. **压缩图片**后再读（例如缩到 400px 宽）—— 能读几张？
2. **只读图片的一部分**（裁剪出标题区域）—— 能读吗？
3. **用 `out/幻灯片总览_高清版.html`** 里的内嵌图片（base64）—— 能读吗？
   （注意：这个文件 6 MB，**先剥离 base64 再读文本部分**，别整个读）

---

## 测试 5 · 文字路线对比（作为兜底验证）

**不用图片**，只读文字，看能不能还原幻灯片内容：

```bash
# 看 102 张幻灯片的 OCR 文本
head -80 out/slides_unique.txt

# 看某几帧的逐帧 OCR
python3 -c "
import json
rows=[json.loads(l) for l in open('out/ocr_all.jsonl',encoding='utf-8') if l.strip()]
print('总帧数:', len(rows))
for r in rows[:5]:
    print(r['file'], r['t'], repr(r['text'][:80]))
"
```

**回答**：
1. `out/slides_unique.txt` 里有几张幻灯片的内容？内容质量如何（能看懂吗）？
2. `out/ocr_all.jsonl` 有几条记录？
3. **对比一下**：光看文字，能不能还原出幻灯片上讲的知识点？
   和「真的看图」相比，**差在哪里**？

---

## 最后必须回答这 7 个问题

| # | 问题 | 答案 |
|---|---|---|
| 1 | 我能不能**看见**单张图片里的文字？ | |
| 2 | 一次能读**几张**图？（准确上限） | |
| 3 | 失败那一级的**原始报错**是什么？ | |
| 4 | 有没有替代方案能绕开限制？ | |
| 5 | 光靠**文字**（OCR）能不能还原幻灯片内容？ | |
| 6 | 文字路线和图片路线，**差在哪里**？ | |
| 7 | 对「制作学习资料」这个任务，你建议走哪条路？ | |

**最后一句话，必须明确表态**：

> 我能看见图片里的文字吗：能 / 不能。
> 我一次最多能读 __ 张图。
> 对这个任务，我建议走 **图片路线 / 文字路线 / 混合路线**，理由是 ____。

---

## 汇报格式

把结果写成 `out/TEST_IMAGES_RESULT.md`，然后：

```bash
git add -A && git commit -m "test: 读图能力测试结果"
git push -u origin HEAD
```
