# TEST_CHEAP.md — 小样测试（**只做一个知识点，验证流水线**）

> ## 这是一个「小样测试」，不是完整任务
>
> **只做一个小样**：用 4 张幻灯片对应的素材，做一页学习内容。
>
> **目的**：验证你能不能
> ① 读对素材 ② 用上读图记录里的手写批注 ③ 产出通俗好懂的内容
>
> **成本很低** —— 只需要读 4 张幻灯片对应的素材。

---

## 一、任务

**做一个小样：「奥苏贝尔的两把尺子」**

**对应范围**：幻灯片 **S001 ~ S004**（视频 00:43 ~ 12:04）

**产出**：一个单文件 HTML，文件名 `out/TEST_SAMPLE.html`

---

## 二、只读这些（**别多读**）

```bash
git fetch origin && git merge origin/main

# ① 读图记录里 S001~S004 的部分
python3 -c "
import re
s = open('out/slides_vision.md', encoding='utf-8').read()
for i in range(1, 5):
    m = re.search(rf'## S{i:03d}([\s\S]*?)(?=\n## S|\Z)', s)
    if m: print('## S%03d' % i + m.group(1))
"

# ② 文字稿里 00:43 ~ 12:04 的部分（老师讲这两把尺子）
head -80 out/transcript.txt

# ③ 幻灯片 OCR 里 S001~S004
python3 -c "
import re
s = open('out/slides_unique.txt', encoding='utf-8').read()
for i in range(1, 5):
    m = re.search(rf'=== S{i:03d} \[[\d:]+\] ===([\s\S]*?)(?==== S)', s)
    if m: print('=== S%03d ===' % i + m.group(1))
"
```

**就这三处。** 不用读别的文件。

---

## 三、要做什么

做一页讲清「奥苏贝尔怎么给学习分类」的内容，**必须包含**：

| # | 要求 | 说明 |
|---|---|---|
| 1 | **说清两个维度** | 接受/发现 × 机械/有意义，说明它们**互相独立** |
| 2 | **至少 2 个生活比方** | 要让人一看就懂，不要用教育学术语解释教育学术语 |
| 3 | **至少 1 条记忆钩子** | 比如口诀、对比、一句话总结 |
| 4 | **用上手写批注** ⭐ | 必须包含 S002 的**红笔坐标轴**（纵轴「关系」/ 横轴「方式」） |
| 5 | **配 1 道真题** | 从 S003 或 S004 里选一道，附**老师的解题步骤** |
| 6 | **自测 1~2 题** | 让考生先想，再看答案 |

**形式**：单文件 HTML，可以直接双击打开。
**风格**：干净、大字、好读（参考 Apple 的设计语言）。

---

## 四、汇报（**必须逐项填**）

| # | 项目 | 结果 |
|---|---|---|
| 1 | 我读了哪几个文件 | |
| 2 | **我从 `slides_vision.md` 里用上了什么**（具体列出来） | |
| 3 | 产出文件与大小 | |
| 4 | 用了几张幻灯片的内容（应该是 S001~S004） | |
| 5 | 有没有哪处看不懂或不确定 | |

**最后一句话**：
> 我读完了 __ 个文件，用上了 `slides_vision.md` 里的 __ 处手写批注信息，
> 产出了 `out/TEST_SAMPLE.html`（__ KB）。
> 本小样**完成 / 未完成**，未完成是因为 ____。

---

## 五、提交

```bash
git add out/TEST_SAMPLE.html
git commit -m "test: 小样测试 —— 奥苏贝尔的两把尺子"
git push -u origin HEAD
```

**只新增这一个文件**，不要改其他任何文件。

---

## 六、这是测试，所以

- ❌ 不要做完整资料
- ❌ 不要读 `slides_102.zip` 里的原图（**这个测试只验证文字流水线**）
- ❌ 不要碰 `scripts/`、`setup.sh`
- ✅ 只看 S001~S004 的素材
- ✅ 做完就停，汇报

**如果你发现素材里有看不懂或矛盾的地方，如实说，不要编。**
