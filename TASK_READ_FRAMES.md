# TASK_READ_FRAMES.md — 读取全部 691 帧（**增量记录版**）

> ## 🔴 本次只读「一个批次」，读完就停
>
> 批次范围由用户在对话里告诉你，例如：
>
> ```
> 读 TASK_READ_FRAMES.md，本次范围：F001~F040
> ```
>
> **读到批次末尾就停下** —— 写入、commit、汇报、结束。
> ❌ 不要继续读下一批（下一批在新会话里跑）

---

## ⭐ 核心技巧：增量记录（**必须用，否则做不完**）

691 帧是**按时间排好序**的，相邻帧**绝大多数几乎一样**（同一页 PPT 的连续画面）。

所以**不要每帧都写完整描述**，而是：

```markdown
## F001（000027.jpg）

**画面**：完整描述（这是新的一页）

---

## F002（000053.jpg）

**与上一帧的差异**：无（同一页）

---

## F003（000055.jpg）

**与上一帧的差异**：换了新页 → 标题「学习与学习理论」

**画面**：完整描述

---
```

**规则**：
- **画面变了** → 写完整描述（按下面的格式）
- **画面没变** → 只写「与上一帧的差异：无」
- **只是多了一行/一个箭头/一处高亮** → 只写增量：「新增：第 3 行『连锁学习』出现」

**这样能省掉 70% 以上的重复劳动。**

---

## 一、准备

```bash
# 同步（必须！工作区不会自动拉新文件）
git fetch origin && git merge origin/main

# 解压 691 帧
mkdir -p /tmp/fr && cd /tmp/fr
unzip -q /path/to/out/frames/frames_unique_691.zip -d frames/
ls frames/*.jpg | wc -l          # 必须等于 691
ls frames/*.jpg | head -3        # 看文件名格式（形如 000027.jpg）
```

**断点续跑**：先看 `out/frames_vision.md` 已记到第几帧。

```bash
grep -o "^## F[0-9]*" out/frames_vision.md 2>/dev/null | tail -3
```

**如果文件不存在** → 新建，从第一帧开始。

---

## 二、逐帧读取

用 **`functions.shell` 的 `image_path`**（**一次一张**）：

```
functions.shell(command="ls frames/000027.jpg", image_path="/tmp/fr/frames/000027.jpg")
```

> ⚠️ **不要用 `functions.display_multimedia`** —— 只返回链接，不提供视觉输入。
> 详见 `HOW_TO_READ_IMAGES.md`。

**编号规则**：按文件名排序，第 1 张记为 `F001`，第 2 张记为 `F002`……

### 画面变了时，写完整格式：

```markdown
## F003（000055.jpg）

**时间**：（如果能从文件名或上下文推断）
**标题**：
**印刷内容**：（照抄；表格按行列）

**手写批注**：
- 位置 / 颜色 / 内容（**尽量逐字抄；认不准写「认不准」**）

**颜色与高亮**：
**版式结构**：
**插图与图标**：

**与上一帧的差异**：换了新页

---
```

### 画面没变时，只写：

```markdown
## F004（000058.jpg）

**与上一帧的差异**：无（同一页，画面完全一致）

---
```

---

## 三、每批读多少

**建议每批 30~40 帧**（用了增量记录后，实际工作量比想象小）。

**吃力就停**，不要硬撑。**断点续跑会自动接上。**

| 参考分批 | 帧范围 |
|---|---|
| 第 1 批 | F001 ~ F040 |
| 第 2 批 | F041 ~ F080 |
| 第 3 批 | F081 ~ F120 |
| … | … |
| 第 18 批 | F681 ~ F691 |

（只是参考。某批只读了 25 帧，下次就从第 26 帧接着来。）

---

## 四、本批完成后

1. **自检**：
```bash
grep -c "^## F" out/frames_vision.md
grep -o "^## F[0-9]*" out/frames_vision.md | sed 's/## //' | sort -c && echo "✅ 顺序正确" || echo "❌ 顺序乱"
```

2. **commit + push**：
```bash
git add out/frames_vision.md
git commit -m "feat: 691 帧读图（FXXX~FYYY）"
git push -u origin HEAD
```

3. **汇报**：

| # | 项目 | 结果 |
|---|---|---|
| 1 | 本次范围 | F___ ~ F___ |
| 2 | 实际读了几帧 | |
| 3 | **本次实际读到第几帧**（断点） | F___ |
| 4 | 其中「画面变了」的有几帧 | |
| 5 | 顺序检查 | 通过 / 不通过 |
| 6 | **如果没做完，卡在哪**（必答） | |

**最后一句话**：
> 本次读完 __ 帧（F___ ~ F___），累计 __ / 691。
> 本批**完成 / 未完成**，未完成是因为 ____。

---

## 五、全部读完之后

新建 `out/FRAMES_GAPS.md`，汇总**所有帧里 OCR 没有、但看图看到**的信息
（按手写 / 颜色 / 结构 / 插图四类）。

---

## ⚠️ 最后提醒：这件事很花时间

- 691 帧 ÷ 每批 30~40 帧 = **约 18~24 批**
- 每批约 15~25 分钟 → **总计 5~9 小时**

**如果你中途觉得不值，随时可以停。**
**已经读到的部分都是有效成果**（断点续跑，不会浪费）。

**而且**：`out/slides_102.zip` 里的 102 张**就是这 691 帧的子集**
（102 张是从 691 帧里挑出来的唯一幻灯片）。
**所以只读 102 张也能覆盖所有内容** —— 读 691 帧是"更彻底"，不是"必须"。
