# HOW_TO_READ_IMAGES.md — 怎么读图片（**重要，开工前必读**）

> **结论先行：本沙箱是能读图的。** 关键是用对工具。

---

## 一、能读图的工具：`functions.shell` 的 `image_path` 参数

```python
functions.shell(
    command="...",
    image_path="/tmp/work/slides/S050.jpg"     # ← 关键：这个参数把图片作为视觉附件返回
)
```

**效果**：图片会作为**视觉附件**进入你的上下文 —— 你能真正「看到」像素，包括：

- 颜色（高亮底色、手写笔迹的颜色）
- 版式（表格结构、箭头、连线关系）
- 手写批注
- 插图、图标、水印
- 小字、角标

---

## 二、**不能**读图的工具：`functions.display_multimedia`

```
❌ functions.display_multimedia
```

它只返回一个 URL 和 Markdown 链接：

```json
{"url":"https://api.hoplite.sh/api/attachments/<id>?w=1200&h=556",
 "markdown":"![S050 slide](https://api.hoplite.sh/api/attachments/<id>)"}
```

**这是「把图片发布给用户看」的工具，不是「读给模型看」的工具。**
用它你会以为"看不见图"，其实是工具用错了。

---

## 三、标准操作流程

```bash
# 1. 解压（只需一次）
mkdir -p /tmp/work && cd /tmp/work
unzip -q /path/to/out/frames/slides_102.zip -d slides/
ls slides/*.jpg | wc -l          # 预期 102

# 2. 逐张读取 —— 用 image_path 参数
#    functions.shell(command="ls slides/S001.jpg", image_path="/tmp/work/slides/S001.jpg")
```

**一次读一张。** 读完立刻把描述写进文件（落盘），再读下一张。

---

## 四、读图时要「看」什么（OCR 丢掉的正是这些）

读每一张时，**除了抄录文字，重点记录**：

| 要记录 | 为什么 |
|---|---|
| **手写笔迹** | OCR 完全抓不到。老师是边写边讲的，这些是解题关键 |
| **颜色与高亮** | 黄色高亮 = 重点；红笔 = 强调/答案；蓝笔 = 补充 |
| **版式结构** | 树状图的层级、表格的行列、流程图的箭头方向 |
| **连线与箭头** | OCR 会把"箭头关系"压平成散行 |
| **插图与图标** | 狗、猫、铃铛、迷宫等示意图（它们承载概念） |
| **角标与页码** | `B81`/`B86` 这类是讲义页码，便于对照 |
| **水印与 logo** | 识别用，不用记录细节 |

**一句话：凡是「用文字说不清」的，都要记。**

---

## 五、注意事项

1. **一次一张，别贪多。** 批量注入不支持，一张一张来最稳。
2. **读完立刻落盘。** 别攒在上下文里，否则后面会爆。
3. **手写字认不准就写「认不准」。** 不许编 —— 宁可标「此处字迹不清」，
   也不要猜一个看起来合理的字。**这是诚信底线。**
4. **不许用 OCR 文本「补答」你看见的东西。** 你可以**对照**（OCR 帮你确认印刷体），
   但不能**用 OCR 代替眼睛** —— 那样会漏掉手写和颜色。
5. **读完后对照 `out/slides_unique.txt`**：OCR 有的、你没看到的 → 回去补看；
   OCR 没有的、你看到的（手写/颜色/结构）→ **这些才是你的核心价值**。

---

## 六、验证方法（想知道自己是不是真看见了）

拿 **S050** 试：

```
用 functions.shell 的 image_path 读 slides/S050.jpg，然后回答：
① 第 4 行下方有没有手写？什么颜色？写的是什么？
② 第 2 行"食物+铃声"那格里，"xN次"是什么颜色？有没有下划线？
③ 右上角有什么？右下角有什么？
```

**正确答案（已验证）**：
- ① 有**紫色**手写，写着「条件S₁ / 中性S / 条件反1」，最右边还有「XN-」
- ② 「xN次」是**红色**，**带红色下划线**
- ③ 右上角：**女老师的视频画面** + `B86` + 超格 logo；右下角：**淡色的卡通狗线稿**

**这些在 `slides_unique.txt` 里一个字都没有。** 答得出来 = 你真的看见了。
