# TEST_READ_ALL.md — 读图流水线测试（**先小批量试跑，别一上来就 102 张**）

> ## 这是测试任务
>
> **只做测试，不要制作学习资料、不要改现有文件。**
> **不许编造。** 看不见就说看不见，认不准就说认不准。

---

## 第一步 · 前置检查（**必须通过才能继续**）

先读 `HOW_TO_READ_IMAGES.md`，然后：

```bash
mkdir -p /tmp/rt && cd /tmp/rt
unzip -q /path/to/out/frames/slides_102.zip -d slides/
ls slides/*.jpg | wc -l          # 预期 102
```

用 **`functions.shell` 的 `image_path`** 读 `slides/S050.jpg`，回答三问：

1. 第 4 行（"铃声 + 灯光 → 流口水"那行）**下方**有没有手写？什么**颜色**？写的是什么？
2. 第 2 行"食物 + 铃声"那格里，「xN次」是什么颜色？有没有下划线？
3. **右上角**有什么？**右侧**有什么？

> ⚠️ **严禁查 `out/slides_unique.txt` / `out/ocr_all.jsonl`。**
> 这三问的答案在 OCR 里**一个字都没有** —— 只能靠眼睛。

**如果这三问答不出来 → 停止，如实汇报"前置检查未通过"。**
**通过了 → 继续第二步。**

---

## 第二步 · 小批量试跑（10 张）

**用 `functions.shell` 的 `image_path`**，逐张读 `S001.jpg` ~ `S010.jpg`。

**一次一张**，每读完一张，**立刻**把结果追加写入 `out/slides_vision_TEST.md`。

### 每张要记录的内容

```markdown
## S001

**画面类型**：（PPT 幻灯片 / 其他）
**标题**：（图上最大的标题文字）
**页码角标**：（如 B81；没有就写"无"）

**印刷内容**：（照抄图上的印刷文字，尽量完整）

**手写笔迹**：
- 位置：（在哪个区域）
- 颜色：（红 / 紫 / 蓝 / 黑）
- 内容：（逐字辨认；**认不准的字写「认不准」，不要猜**）

**颜色与高亮**：
- 哪块有高亮底色？什么颜色？
- 哪些字是红笔？

**版式结构**：
- （树状图 / 表格 / 流程图 / 纯文字）
- （如果是图表，描述层级和箭头关系）

**插图与图标**：
- （画的是什么：猫 / 狗 / 铃铛 / 迷宫 / 其他）

**和 OCR 对比**：
- OCR（`slides_unique.txt`）里**有**、我**没看到**的：____
- 我**看到**、OCR 里**没有**的：____   ← **这些是你的核心价值**
```

**每读完 5 张就 `git add` + `git commit` 一次**（防止上下文爆掉丢进度）。

---

## 第三步 · 质量自检（**关键，别跳过**）

10 张读完后，自己检查：

1. **10 张都读到了吗？** `grep -c "^## S" out/slides_vision_TEST.md` 应该是 10
2. **有几张抓到了 OCR 没有的信息？**（手写 / 颜色 / 结构）
   - 如果 **0 张** → 说明你其实没在"看图"，只是在抄 OCR，**如实汇报**
   - 如果 **≥ 3 张** → 说明读图有效
3. **手写认错的概率高吗？** 你自己判断，如实说

**举一个你抓到的「OCR 没有、但你看到了」的具体例子**（这是最重要的证据）。

---

## 第四步 · 汇报这 6 个问题

| # | 问题 | 答案 |
|---|---|---|
| 1 | 前置检查（S050 三问）通过了吗？ | |
| 2 | 10 张都读完了吗？实际读了几张？ | |
| 3 | 有几张抓到了 OCR 里没有的信息？ | |
| 4 | 举一个具体例子（OCR 没有、你看到了） | |
| 5 | 读 1 张大概要多久？读 102 张预计要多久？ | |
| 6 | 你觉得读 102 张可行吗？有什么风险？ | |

**最后一句话必须明确表态**：

> 我能不能真正「看到」图片内容：能 / 不能。
> 读 10 张的实际效果：有效 / 部分有效 / 无效。
> 读 102 张是否可行：可行 / 需分批 / 不可行，理由是 ____。

---

## 第五步 · 提交

```bash
git add -A
git commit -m "test: 读图流水线测试（10 张试跑）"
git push -u origin HEAD
```

**注意**：
- 只提交 `out/slides_vision_TEST.md` 这一个新文件
- **不要改** `out/` 下已有的任何文件
- **不要**跑 setup.sh、不要 OCR、不要转写
